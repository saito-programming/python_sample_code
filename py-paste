#!/usr/bin/python3
import os
import getpass

# py-copy と同じ一時ファイルパスを特定
user = getpass.getuser()
clip_file = f"/tmp/py_clip_{user}"

# ファイルが存在すれば中身を出力、なければ何も出力しない
if os.path.exists(clip_file):
    with open(clip_file, 'r') as f:
        # 末尾に余計な改行を入れず、保存されたデータだけを出力
        print(f.read(), end='')
